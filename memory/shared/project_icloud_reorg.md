---
name: project_icloud_reorg
description: "Reorganizing F:\\iCloudDrive via a local, phased copy-clean-dedupe workflow (project scripts at C:\\Users\\ohjos\\iCloudReorg)"
metadata: 
  node_type: memory
  type: project
  originSessionId: e45c2b11-7288-4b60-b119-01000d1b779e
---

User is reorganizing **F:\iCloudDrive** (~90 GB, 55k files). Direct edits in iCloud break/undo things, so the approach is: work on a LOCAL copy, then (later) decide whether to push back to iCloud or migrate off. Started 2026-07-03.

**Why:** iCloud placeholder sync mangles bulk in-place changes; content reads on online-only files trigger mass downloads.
**How to apply:** All paths live in `C:\Users\ohjos\iCloudReorg\config.json` (configurable per machine, per [[feedback_cross_device_paths]]). Stage folders use distinctive `_v1` names to avoid Windows collisions, per [[feedback_windows_folder_collisions]].

Phased plan (gated — build+run one phase at a time):
- **Phase 0 DONE**: `1_scan.ps1` (metadata-only, no downloads) → `2_analyze.py`. Output in `inventory\` + `reports\`.
- **Phase 1 DONE (2026-07-03)**: `3_mirror.ps1` (has `-ReportOnly` pre-flight) mirrored 54,559 files / 88.59 GB → `stage_raw_mirror_v1`, robocopy exit 1, ZERO failures. `.Trash` excluded. Gotcha fixed: inside `$list.Add("..." -f a, b)` the method-call commas swallow the `-f` args — wrap the format expr in its own parens.
- **Phase 2 DONE (2026-07-03)**: `4_verify.py` (full SHA-256 both sides) = GATE PASS, 54,559/54,559 byte-identical, 0 missing/mismatch/error. Mirror is a proven bit-perfect backup; iCloud deletion now recoverable (still deferred). Produced `inventory\mirror_hashes_*.csv` for Phase 3 reuse. GOTCHA: the tree contains 2 files literally named `nul` (Windows reserved device name, from `> nul` redirects, under IL_work\...\ProjectTwo_LocalLLM and \Personal\Resume). Python `os.path.abspath`/`relpath`/`normpath` resolve `nul` to the `\\.\nul` device and crash. Fix: use the `\\?\` extended prefix WITHOUT normalizing, and build relative paths by string-slicing the root, never relpath. Phase 3 hashing must do the same.
- **Phase 3 DONE (2026-07-03)**: `5a_dedupe_report.py` (report-only exact-dup analysis: 724 groups/1,299 extras/988MB, mostly books) DONE. `5b_reorganize.py` (OOP: FileRecord→Classifier(Rule chain)→Destination, DedupeIndex keeper=shallowest, ReorgPlanner→ReorgPlan) builds a DRY-RUN plan by default (`--execute` to copy mirror→`stage_working_v1`). Taxonomy: 00_Projects (atomic, no dedupe) / 01_School_UoU / 02_Books_Library (by subject) / 03_Finance_Taxes / 04_Documents / 05_Photos / 06_Video / 07_Audio / 08_Archives / 09_Misc_Unsorted / _Quarantine_Duplicates. Plan: 54,559 files, only 166 misc, 287 dups→quarantine (867MB). Books classified 96.5% by subject after adding underscore-normalized keyword matching. NOT yet executed.
- Phase 4 IN PROGRESS: cutover = delete-old-first then upload, categories at root. PRESERVE (never touch on iCloud): the 5 app-container folders (`~` in name: Readdle/Pages/Preview/GarageBand/Workflows), `.Trash`, `desktop.ini`. Upload everything incl. node_modules (user's call). Scripts: `6a_cutover_preflight.py` (done), `6b_sync_check.py` (MANDATORY pre-delete gate — user-required), plus 6c delete / 6d upload (not yet built).
- **KEY EVENT (2026-07-03): the mandatory sync check CAUGHT DRIFT and blocked deletion.** iCloud is actively used — between Phase 1 mirror and cutover, 118 files were ADDED (notably a whole `Documents\Bank Statements\Charles Schwab\` folder) and 24 removed (some dated same day). So the v1 mirror AND working tree were stale. LESSON: always run `6b_sync_check.py` immediately before any delete; never trust an older mirror. Refreshing to **v2** (config repointed to `stage_raw_mirror_v2`/`stage_working_v2`, v1 kept as fallback) via `refresh_v2.ps1` (mirror→verify→reorg→sync-check, fail-fast). Only after sync-check PASS may delete+upload proceed. **v2 refresh DONE (2026-07-04 00:22): sync check PASS** (live iCloud 54,638 == v2 mirror, 0 new/mod/removed). working_v2 rebuilt (54,653 files, 0 errors). Cutover cleared — pending 6c/6d build + explicit run confirmation. Scripts committed to a git repo at C:\Users\ohjos\iCloudReorg (stage_/inventory/logs/reports/rescued_from_trash/__pycache__ gitignored to keep personal filenames out). **Now pushed to PRIVATE GitHub remote github.com/JoshuaZayne/iCloudReorg (branch master, origin tracking set) as of 2026-07-04** — plain `git push` for updates.

**MAJOR PIVOT (2026-07-03): user had already intentionally cleaned up iCloud — DON'T re-upload the old snapshot.** A fresh `run_all.ps1` scan found **~86 GB / 46,453 files now sitting in `F:\iCloudDrive\.Trash`** (GoPro 49.9GB, Baby Birth.MOV 23.3GB, BOOKS, MUSIC, Voice memos, most of Documents/Desktop). Only ~4.46 GB / 8,384 files remain LIVE (IL_work, UoU_school, TRADING, STATA, loose files). The mirror was built pre-cleanup, so `stage_working_v2` = the OLD 88GB and must NOT be uploaded (would resurrect trashed data). User confirmed the trashing was intentional. **Safety-model gap exposed:** `6b_sync_check.py` excludes `.Trash` on BOTH sides, so a mass move-to-trash scores as "removed=warn only" and would still PASS — do not trust a PASS alone after a big cleanup; always eyeball the scan's .Trash size. **`stage_raw_mirror_v2` already backs up the big trashed items** (verified Baby Birth/GoPro/BOOKS/MUSIC/Bank Statements present) — KEEP v2 as the pre-cleanup archive. The one item NOT in the mirror was `Voice memos_` (2.07GB, 119 real-estate .m4a notes) → **RECOVERED to `rescued_from_trash\Voice memos_` (gitignored), verified 119/119 byte-match.** New plan: **v3 = organize LIVE-only ~4.5GB.** config.json repointed raw_mirror/working/quarantine to `_v3` (v2 kept on disk as archive; see config `_archive_note`). Leave Trash to expire. **CUTOVER DONE (2026-07-03):** run_all v3 passed all gates (verify + sync-check PASS, reorg 8,356 organized/28 quarantined/0 err). `6c` deleted 16 live root items (0 failures, incl. clean nul-file delete + 2 empty Desktop/Documents shells); `6d` uploaded 6 categories (00_Projects/01_School_UoU/04_Documents/05_Photos/08_Archives/09_Misc_Unsorted), all byte-for-byte verified F: vs working_v3. iCloud root now = 6 NN_ categories + .Trash + app-containers + desktop.ini. iCloud still uploading to Apple servers in background; keep stage_raw_mirror_v3 until tray shows synced. Note: live data was already tidy so the reorg mostly renamed IL_work→00_Projects & UoU_school→01_School_UoU and buckets small stuff under 04_Documents (TRADING/STATA fragmented by type under their category); user chose to proceed anyway.

**Hardening done this session (committed-pending):** `3_mirror.ps1` now `exit`s ≥8 on robocopy failure (was always 0). `5b_reorganize.py` gained `--clean` (reserved-name-safe tree teardown via `\\?\`+scandir) + copied/skipped/error accounting + non-zero exit on mismatch (was silently skipping existing files → stale working tree on rerun). `6c_delete_old.ps1` deletes via `Remove-TreeRobust` (falls back to `cmd rd/del \\?\` for `nul` reserved-name folders, verifies gone, exit 1 on residual). `6d_upload.ps1` renamed `$args`→`$roArgs`, aggregates failed categories, exit 1. NEW `run_all.ps1` = fail-fast top-to-bottom orchestrator (scan→analyze→mirror→verify⛔→dedupe→reorg --clean --execute⛔→preflight→sync-check⛔), config/PSScriptRoot paths, `-SkipScan`. `refresh_v2.ps1` now a thin wrapper over it. NEW `ARCHITECTURE.md` documents the whole process/gates/cutover.

**DELTA RE-SYNC (2026-07-08): Documents typed + duplicate cleanup pushed to iCloud.** User asked to reconcile local↔cloud, kill duplicates, and give 04_Documents type folders. Findings: the 6 NN_ categories were already in sync (counts identical); the "duplicates" were (a) the local-only `_Quarantine_Duplicates` holding one extra per real file, and (b) app-container content copied into the organized tree so it lived in BOTH the preserved root container AND a category (e.g. `04_Documents\com~apple~Pages\Blank.pages`, `05_Photos\com~apple~Preview\Untitled.png`, Readdle stamps/plists in 08/09, the nested com~apple~Pages). Changes to `5b_reorganize.py` (committed 5980fab, local master, NOT pushed to GitHub yet): new **DocumentTypeRule** — loose docs → `04_Documents/<Type>/` (PDF/Word/Excel/PowerPoint/CSV/Text/Other), grouped docs keep their named folder under `04_Documents/_Groups/<folder>/` (VA LES Statements/TRADING/STATA Analysis/TrexQuant preserved); **app-container exclusion** (`is_app_container`: any `~` in top-level → dropped from plan entirely, they're preserved in place); **0-byte dedupe skip** (empty files share one hash, must not be deduped against each other). Rebuilt working_v3 (8,370 files, 0 err), deleted `_Quarantine_Duplicates`. **IMPORTANT — the old 6b/6c cutover path does NOT work for a re-sync:** 6b_sync_check compares live iCloud vs the RAW mirror, but live is already in NN_ layout, so it always FAILs now → 6c would abort. Instead did a surgical **per-category `robocopy /MIR`** (flags `/COPY:DAT /DCOPY:DAT /R:3 /W:10 /MT:4`) of the 5 changed categories (01/04/05/08/09; skipped 00_Projects, identical). Gated it with a read-only metadata diff (basename+size, no hashing→no downloads) + `robocopy /L` dry-run first; verified every purge was either a relocation or app-container content still present in its preserved container. Post-sync: all 6 categories live==work. iCloud now uploading in background. Empty `Documents\`/`Desktop\` root shells remain (iCloud D&D auto-recreates them; left alone). Reusable diff/rm helpers were one-off scratchpad scripts, not committed.

**FULL ROLLBACK / RESTORE (2026-07-09): user said the reorg "messed up" iCloud — restored the ORIGINAL first mirror (v1) in full, by explicit informed choice.** After the 2026-07-08 delta re-sync, user was unhappy and asked to wipe iCloud and put back the original documents from the first mirror. Safety check first: diffed live iCloud vs v1/v2 by (name,size) — only 1 file on live not in the mirrors (a `desktop.ini`), so restore could lose nothing real. Presented the scope fork (v1 is the pre-cleanup 88.6 GB incl. the ~84 GB GoPro/Desktop-video/BOOKS/MUSIC the user had intentionally trashed July 3); **user explicitly chose FULL 88.6 GB restore knowing it resurrects the trashed media.** Guard-checked v1 (54,559 files, 89 GB, intact) then `robocopy /MIR stage_raw_mirror_v1 -> F:\iCloudDrive /XD .Trash /COPY:DAT /DCOPY:DAT /R:2 /W:5 /MT:8`. Result: Copied 54,545 / Skipped 14 / FAILED 0 / purged 8,347 extras (the NN_ organized tree). Verified both directions: live (excl .Trash) == v1 == 54,559 files, 0 missing, 0 extra — **PERFECT MATCH.** iCloud root is now the ORIGINAL layout (BOOKS/Contracts_/Credit card statements/Degrees/Desktop/Dimaskus/Documents/Downloads/GoPro Photos/Health Insurance/IL_work/Icloud Job Folder/STATA/TI-Nspire/TRADING/TrexQuant/UoU_school/Zayne + app containers + loose files), NOT the 6 NN_ categories. iCloud is now re-uploading ~88 GB to Apple in the background — KEEP stage_raw_mirror_v1/v2 until the tray shows fully synced. **LESSON: the user did not want their iCloud reorganized into the NN_ taxonomy on the live drive; do not re-push any organized/working tree to iCloud without a fresh explicit go.** Also found (user asked): VA "percentage/pay" benefits letters are at `Documents\IMPORTANT DOCUMENTS_\` (Benefit Summary Letter 2025.pdf, Benefit Verification Letter with payment 22NOV2025.pdf, Full Benefit Summary Letter 22 NOV 2025.pdf), `Documents\DoD\Benefit Summary Letter Joshua Johnson_.pdf`, and `UoU_school\...\Spring 2022\VA documents_\Benefit Summary Letter with amount and percent.pdf`.

Phase 0 findings: two populations — (1) a few huge media files = nearly all GB (GoPro DCIM 49.9 GB, one 23 GB Desktop .mov, video 72 GB/80 files); (2) dev-project internals (node_modules/.git/.lzma, ~28k .js/.ts files) = nearly all the file count but trivial size, and they inflate the "25k duplicates" number to a meaningless 839 MB reclaimable. Real dupes worth acting on = ~30-40 large book PDFs duplicated across BOOKS\ subfolders. C: has 785 GB free (no space constraint).
