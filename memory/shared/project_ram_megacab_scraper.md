---
name: project_ram_megacab_scraper
description: "Running & extending the RamMegaCabDataScrapper (nationwide RAM 2500/3500 Mega Cab Limited finder), incl. per-state coverage architecture and ops gotchas"
metadata: 
  node_type: memory
  type: project
  originSessionId: 468393e6-4a1e-4823-b465-9c38783b1153
  modified: 2026-08-09T01:08:50.193Z
---

**>>> READ `docs/FINDINGS.md` FIRST (added 2026-08-08). <<<** It is the consolidated, measured record of what is actually broken vs only apparently broken: the parseability-vs-reachability bug class (`SUPPORTS_HUMAN=False` barring a source from the only tier that could reach it, then demoting it for yielding nothing — hit TrueCar States, iSeeCars States, Cars.com States), why the challenge taxonomy was recording nothing (`challenges.json` did not exist; `safe_get` discarded every non-200 body so only 200-served walls were ever diagnosed), the TWO different cars.com walls (fast tier 403 `ip_block` wants a new exit IP; browser tier 200 "Just a moment" `js_challenge` wants a cleared session), how to read a wall out of `output/cache/html_cache/*.json` via `fp_len` + `fingerprint` (visible text, no HTML stored), and the environment gotchas (PowerShell 5.1, .NET relative paths resolving to the process CWD, `"cars.com" in url` matching `iseecars.com`). Prefer updating that file over growing this memory.

Repo: `F:\GitHub Repos\RamMegaCabDataScrapper` (GitHub JoshuaZayne/RamMegaCabDataScrapper). **MOVED 2026-07-20** from `C:\Users\ohjos\repos\RamMegaCabDataScrapper` to the F-drive repos folder (`F:\GitHub Repos\` is where all the user's git repos live). Full tree incl `.git`/`.env`/`secrets/`/`output/` robocopied, git fsck clean, same HEAD `46defcf`, branch `fix/vin-trim-decode` still ahead 2; C: original deleted. The daily Scheduled Task "RamMegaCabScraper Daily" was repointed to the F `scheduled-run.ps1` (scripts use `$PSScriptRoot`, no hardcoded repo paths inside). Docker-sandboxed scraper; hunts RAM 2500/3500 Mega Cab in Limited / Limited Longhorn nationwide, buckets results per state.

**2026-07-20 — clarity refactor (main-folder + supporting-folders layout).** The single command is now **`GET-TRUCKS.bat`** (double-click → full scrape → `output\trucks.xlsx` which now **auto-opens** when an interactive run finishes; `run.bat` kept as an alias). The `.bat` files **pause at the end** (window stays open on double-click; shows failure hint + log paths). **No Desktop copies anymore** — all outputs stay in `output\` only (user asked for this). Auto-open gated on `-not $NoBoard` so headless/scheduled runs never pop Excel.

**Folder layout (final):** entry points at root (`GET-TRUCKS.bat`, `run.bat` alias, `run.ps1` engine, `scheduled-run.ps1`, `tangle.py`); **`scripts/`** holds ALL supporting scripts by purpose — `scripts/setup/` (`capture-session.ps1`+`.bat`, `install-daily-run.ps1`), `scripts/dev/` (`preflight.py`, `litcode.py`, `check_session.py`, `coverage.py`, `audit_workbook.py`, `summary.ps1`, `demo_events.txt`), `scripts/routing/` (path_probe / build_routing_table / train_routing_tree), `scripts/archive/` (dead `gen_batch2-9.py` + superseded `capture_session.*`/`schedule_daily.ps1`); **`build/`** (Dockerfile + docker-compose.yml + requirements*.txt) stays top-level. `scraper/` stays at root (literate-code system keys off it). All moves were `git mv`; daily task still targets root `scheduled-run.ps1`. NOTE: `scripts/setup/*.ps1` walk TWO levels up for repo root; `scripts/dev/preflight.py` finds repo root by walking up to the `scraper/` dir.

**2026-07-20 — preflight/host-Python fix + logging (commit f9b216f).** GET-TRUCKS failed on the user's machine with `preflight failed` = `ModuleNotFoundError: tenacity`: `run.ps1` ran host-side `tangle`+`preflight` on whatever `python` was first on PATH (often one WITHOUT the scraper libs; the real scrape is in Docker and self-contained, so a bare host python must never block it). Fix: `run.ps1` now RESOLVES a host python that actually imports the deps (tries `python`, `py -3`, then `%USERPROFILE%\anaconda3\python.exe` — the user's anaconda base 3.9 has everything); if one has deps → run preflight (fail-fast); if not → skip preflight with a clear message and continue to Docker; if no python → skip tangle+preflight too. `preflight.py` now catches `ModuleNotFoundError` → prints `PREFLIGHT SKIP` and exits **2** (vs 1 = real failure); `run.ps1` treats exit 2 as skip. Also: `errors.log` now captures the FULL Python traceback (state machine from `Traceback` line through the final `SomeError:` line), not just the header. Verified via a scoped smoke run (`SCRAPER_ONLY_SOURCES=craigslist SCRAPER_SMOKE=1`): preflight PASS → build → fetch → `output\trucks.xlsx`, exit 0.

**2026-07-20 — speed + concurrency + monolithic runner (pushed, HEAD `e807115`; all session work now on origin/fix/vin-trim-decode).** (1) `run.ps1` SKIPS `docker compose build` when the image exists and nothing in `scraper\`/`build\` changed since it was built (`docker image inspect .Created` vs newest source mtime); `tangle.py` is now idempotent (only writes changed files) so mtimes stay stable and the skip works. Force with `GET-TRUCKS.bat -Rebuild`. (2) End of run prints + logs the Excel + `run.log`/`errors.log` paths. (3) **Concurrency:** committed compose default `SCRAPER_SOURCE_CONCURRENCY` 6→24; fetcher RAM/shm now overridable via `SCRAPER_FETCHER_MEM`/`SCRAPER_FETCHER_SHM` (defaults 3g/512m). The desktop `.env` (gitignored, LOCAL only) sets SOURCE=68 (all sources), BROWSER=4, FETCHER_MEM=6g, FETCHER_SHM=1g — laptop/Pi should delete those 4 lines to keep safe defaults. Browser tier stays separately capped (each headless Chromium ~300-500MB). (4) **`scripts/get_trucks.py`** = one-file stdlib Python equivalent of GET-TRUCKS.bat (docker check → host-python resolve → tangle → gated preflight → build-skip → plan/fetch/analyze → prints paths → opens xlsx → down -v). (5) **`docs/HOW_GET-TRUCKS_WORKS.md`** = full step-by-step trace + speed knobs (linked from README).

**2026-07-20 — never-hang / always-produce-a-workbook (pushed, HEAD `50c366a`).** A source wedged past its budget (e.g. New Car Dealer Networks, browser tier) used to hang the fetch forever (`as_completed` had no timeout) so no Excel was made. Fixes: (a) `cli/fetch.py` writes the manifest INCREMENTALLY (after each source via a `_record` closure) so any interruption is salvageable; (b) overall backstop `SCRAPER_FETCH_BUDGET_S` (config `FETCH_BUDGET_S`, default 3600s) via `as_completed(futs, timeout=...)` — on `FuturesTimeout` it stops waiting on stragglers, keeps completed sources, and `os._exit(0)`s the fetcher container (manual executor mgmt, not `with`, so it can't block joining a wedged thread), so the run still proceeds to analyze → `trucks.xlsx`; (c) `-Salvage`/`--salvage` runs analyze-ONLY on the last fetch's quarantine data (Ctrl-C a hung run, then `GET-TRUCKS.bat -Salvage`). The Excel path is printed on the live board footer (`Workbook -> ...`) AND in a DONE block at the end (run.ps1 `Show-Results` / get_trucks `report_and_open`). GET-TRUCKS.bat failure footer suggests `-Salvage`. Doc: `docs/HOW_GET-TRUCKS_WORKS.md` "if a run hangs or you interrupt it" section.

**2026-07-20 — output/ reorganized into subfolders (commit `9761d5f`, committed local, NOT yet pushed).** Was a flat ~200-file dump; now grouped: **`output/latest/`** (trucks.xlsx/.md, alerts.json/.md — the current deliverables, auto-opened), **`output/runs/`** (trucks_<stamp>.*, alerts_<stamp>.* — every run archived), **`output/logs/`** (run.log, errors.log, timing_*, scheduled-logs/), **`output/state/`** (history.db, method_cache.json, url_health.json, routing_table.json, bandit_state.pt, remedy_store.json), **`output/debug/`** (raw_listings, run_choices, filter_debug, url_analysis, verification.*), **`output/comps/`** (negotiation_comparables.xlsx + sold CSVs), **`output/snapshots/`**, **`output/cache/`** (html_cache). Centralized via **`config.out_sub(NAME)`** helper + `config.OUT_LATEST/OUT_RUNS/OUT_LOGS/OUT_STATE/OUT_DEBUG/OUT_COMPS/OUT_SNAPSHOTS/OUT_CACHE` constants — report.py/excel.py route latest-vs-timestamped; analyze/plan/verify/comps/history/remedy/url_health route by passing the subdir; `routing_table.json` runtime overlay now read+written under `state/`. `.gitignore` simplified to track ONLY `output/latest` + `output/runs` (deliverables); everything else regenerable + ignored (untracked ~500 old snapshots/debug/state files — repo shrink, physical files kept). NOTE: run.ps1/get_trucks.py write logs to `output/logs/`, look for the workbook in `output/latest/trucks.xlsx`. GOTCHA when editing docs/PS1 output paths programmatically: a naive Python string replace turns `output\trucks` into a TAB (`\t` escape) — build the backslash with `chr(92)` or use the Edit tool.

**2026-07-21 — FB-GraphQL + negotiation sheets + live comp verify (uncommitted, on branch fix/vin-trim-decode; tangled + 133 tests pass + smoke-tested).** Five improvements the user asked for (paid unlocker DECLINED, so cars.com/AutoTrader stay walled):
- **FB Marketplace via GraphQL JSON, not HTML.** `strategies/human.py` `_visit` now taps the network for FB (`page.on("response")`), captures GraphQL bodies containing `marketplace_listing_title`, and after the grid loads parses the STRUCTURED JSON (strips `for (;;);`, tries whole body + each line) via new `_common.facebook_listings_from_json` (dedicated FB walker `_fb_walk`/`_fb_price` — the generic walker misses FB because year/model live in the title text, price in `listing_price.amount`). Merges with the HTML card parser (kept as fallback), deduped by /marketplace/item/<id>/ URL. Listener removed each visit.
  - **2026-07-21 — FB now WORKING (live-verified).** Playwright chromium installed (was never fully downloaded); user logged into FB → `secrets/storage_state.json` saved (9 FB cookies, check_session OK); `BROWSER_UA` added to `.env` (Chrome 130 Windows, LF-normalized — Add-Content wrote CRLF which would taint the value via docker --env-file, fixed with `sed -i 's/\r$//'`). Live test against real FB: session logged in (26 cards), 7 GraphQL payloads → **108 marketplace objects** → extractor pulled all 5 RAM-ish with prices/year/trim/body parsed (FB price is `listing_price.amount` = "37900.00" string; `_coerce_price` handles the decimal). GraphQL yields 5x the HTML parser (1). 0 passed strict `accept()` THAT query — correctly (only a 2014 + 2015 below the 2020 floor, and a 2026 Ram 2500 Laramie with no Mega Cab in title) — those now land in the Rejected & Near-Miss sheet. **KEY REALITY: FB search is fuzzy junk** (a "ram 3500 mega cab" query returned Honda Civics, a lawn mower, a skydiving rig); the extractor narrows to ram-ish titles and the strict filter finishes. Yield depends on live FB inventory; real scraper runs many city+query combos and accumulates daily. Session expires ~1-3 mo → re-run `scripts/setup/capture-session.ps1` (or `python -m playwright open --save-storage=secrets\storage_state.json https://www.facebook.com/marketplace/`).
- **"Beat My Offer" sheet** (`excel._write_beat_offer`, right after Top Picks): every Limited/Longhorn Mega Cab priced <= `config.BEAT_OFFER_PRICE` (new env `SCRAPER_BEAT_OFFER_PRICE`, default 87000 = the current AutoNation offer), cheapest first, full columns. The one negotiation tab.
- **"Rejected & Near-Miss" sheet** (`excel._write_rejected`): EVERY hit that failed the filter, with its reject reason (analyze.py now collects `rejected_listings` via `l._reject_reason` and threads it through `stats`). Fulfills "every hit stored in a sheet" — nothing scraped is discarded now (was only aggregate counts in Filter Funnel + raw_listings.json debug). NOTE: openpyxl forbids `/` in sheet titles — use `&` (learned the hard way).
- **Live comp verification** (`comps.live_verify` + `--live-verify`/`--max` CLI flags): re-fetches each comp's live page NOW (requests->urllib fallback, threaded), sets Verify to verified/price-changed/gone/unreachable from reality instead of the stale HTML cache. Use before citing comps at the dealer. Walled pages read "unreachable (live)".
- Workflow reminder: edited the literate `docs/src/**/*.md`, ran `python tangle.py`, `--check` clean. Changes NOT committed yet.

**Docker now runs from build/ (critical invocation):** `run.ps1` calls `docker compose -p rammegacabdatascrapper -f build\docker-compose.yml --env-file .env <cmd>`. Do NOT use `--project-directory` — it made `build.context: ..` resolve one level too high (`F:\GitHub Repos\build`, build failed). Instead the compose file sits in build/ (its own dir = project dir), `context: ..` + `dockerfile: build/Dockerfile` reach repo root, and the host mounts are `../output` / `../secrets`. `.env` is at repo root so it's passed via `--env-file`. Verified 2026-07-20: image builds, planner phase runs, a probe file written to container `/app/output` lands in host `output/`.

**Excel deliverables push to GitHub:** `output/trucks*.xlsx` + `trucks*.md` are tracked (already committed) and now have explicit `!` allow-rules in `.gitignore`. Only `output/negotiation_comparables.xlsx` (regenerable comps) and `~$*` Excel lock files stay ignored.

**Running tests locally (no venv in repo):** system Python 3.9 (`python`) already has torch/numpy/bs4/openpyxl/pytest; only `rapidfuzz`, `playwright`, `playwright-stealth` were missing (`pip install --user`). `python -m pytest -q` = **133 passed, 2 skipped**. `python tangle.py --check` = ok. Local dev install is now `pip install -r build\requirements-dev.txt`.

**>>> HANDOFF / OPEN ITEMS (as of 2026-07-17) <<<** All work is committed AND pushed to branch **`fix/vin-trim-decode`** (latest `4c9ca20`); tree clean except `output/` run artifacts. Last full run = **277 trucks / 19-of-51 states**. Negotiation numbers: 2026-Limited comps median **$91,675**, cheapest **$85,594** (so the user's **$86,500** target on the Colorado truck is fully supported). **NEXT STEPS, in order:**
1. **Facebook session (the one thing that unblocks FB — user action).** FB currently yields 0 because there's NO `secrets/storage_state.json` (logged-out FB = 400 + login modal + a degraded preview). Run **`export-session.ps1`** (installs Playwright, opens a Chromium window → user logs into Facebook, closes it → session saved to secrets/). Then a **dump run**: `$env:SCRAPER_FB_DUMP=1; .\run.ps1 -NoBoard` (optionally also `$env:SCRAPER_ONLY_SOURCES="facebook_marketplace,facebook_marketplace_states"` for a fast FB-only run). That writes the real logged-in FB HTML to `output/snapshots/facebook*__*.html`. THEN: read that HTML and tune `_common.facebook_cards()` (or switch to FB's embedded GraphQL JSON) against the real DOM — the current extractor passed a synthetic test but got 0 on FB's real obfuscated markup. (A local `python -m playwright install chromium` was mid-download when the user shut down — just re-run export-session.ps1.)
2. **Merge PR #4** (`fix/vin-trim-decode` → main). ~16 commits this session (cap removal, State Coverage ledger, breadth-first interleave, platform fingerprinting, prioritized enrichment, daily Scheduled Task, FB fixes, dead-source pruning). Not yet merged; the daily task runs the working tree so it already benefits.
3. **Paid unlocker decision** (user's call, costs money): **ZenRows ~$69/mo Developer (~250K credits)** is the only path to the walled aggregators (cars.com/AutoTrader/CarGurus = every dealer nationwide + the actual Colorado truck). If yes: bump `UNLOCKER_MAX_TOTAL` in `.env` and re-run. Free tier is maxed (~20 states/run; daily task accumulates the union).
4. Offered-but-not-done: pull the 3-4 cheapest 2026-Limited comps (dealer+VIN) into a paste-ready AutoNation email.

Colorado target truck = 2026 Ram 3500 Limited Mega Cab, VIN 3C63R3PL0TG332645, stock TG332645, AutoNation Littleton CO, listed $91,497 (MSRP $104,665), ~109 days on lot, cars.com id `4d4770d1-14c8-4cd2-9a10-5eafcf169648` (URL: cars.com/vehicledetail/<id>/). NOT scrapeable (cars.com walled) — verified manually. See [[project_ram3500_autonation_deal]].


**Run it:** `run.bat` (or `run.bat -NoBoard` for raw stream; sets up live watchdog otherwise). Requires Docker Desktop running. `run.ps1` auto-runs `tangle.py` then docker build → plan → fetch → analyze. Full run with all sources is long (~1–2 hrs; 59 sources as of 2026-07-07). Outputs land in `output/` and are copied to Desktop (trucks.md/xlsx, alerts). Runs are NOT auto-committed.

**Literate code:** source of truth is `docs/src/**/*.md` (four-backtick ```python fence). Edit the `.md`, then `python tangle.py` regenerates the `.py`; `python tangle.py --check` guards drift; `python tools/litcode.py detangle` syncs back if you edited a `.py` directly. New sources: add `docs/src/scraper/sources/<name>.py.md` and register in `docs/src/scraper/sources/__init__.py.md` (both the import block AND `ALL_SOURCES`).

**Per-state coverage model:** every state (50 + DC) is a bucket (`config.TARGET_STATES`). `config.SEARCH_ZIPS` maps each state to its capital-city ZIP — the canonical way to parameterize a source per state. A source module exposes `SOURCE`, `SUPPORTS_HUMAN`, `URLS: list[UrlSpec]`, optional `API_URLS` (JSON/api tier). `UrlSpec(url, state_hint, city_hint)` — `state_hint` stamps the bucket when the card text has no parseable state. Fast/JSON sources (SUPPORTS_HUMAN=False) can cover all 51 states cheaply; browser-tier ones are capped by the 360s per-source budget so keep their URL counts modest.

**2026-07-07 improvement (lifted state coverage 5 → 28 states, 95 → 348 listings):** added 15 new per-state source modules + a smarter auto-accept. Best performers were all fast-tier: TrueCar/eBay/iSeeCars States, Craigslist Nationwide, AutoTempest States. Auto-accept knob `FILTER_ACCEPT_ON_HINT` (filter.py): recovers real Mega Cabs from a mega-cab-scoped search when no CONFLICTING cab appears in the listing's own text, flagged `cab-unconfirmed` (recovered 204). The old strict "mega cab unconfirmed" gate was the #1 killer of real trucks.

**The premium-source lever:** CarGurus/CarMax/Cars.com/AutoTrader, extra search engines, OEM locator, gov auctions fall through to the `unlocker` tier and return 0 UNLESS `UNLOCKER_*` (ZenRows/ScraperAPI/etc.) or `SCRAPER_PROXIES` are set in `.env`. Enabling these is the highest-leverage next step for the remaining zero-states (MI/NY/NJ/PA/VA/TN...).

**Ops gotcha:** stopping a run mid-build (`docker compose down` during image build) can corrupt the BuildKit layer cache — next build fails with `failed to prepare extraction snapshot ... parent snapshot ... does not exist`. Fix: `docker builder prune -af` (+ `docker rmi rammegacabdatascrapper`) then rebuild clean.

**2026-07-11 — cross-run HTML cache lever (`scraper/htmlcache.py`):** new fetch-time short-circuit. Every fetched search page is saved to `output/html_cache/<slug>.html` + `.meta.json` (kept, not deleted) and reused as next run's baseline. The single choke point is `extract_listings_from_html` (`sources/_common.py`): on entry it fuzzy-compares the new fetch to the cached page (stdlib `difflib` ratio over *visible text* — scripts/styles/tags stripped, so ad/session churn doesn't count; `SequenceMatcher(autojunk=False)` is REQUIRED or big pages score ~0.2). If ≥ `HTML_CACHE_SIMILARITY` (0.90) it reuses last run's already-enriched listings and refreshes ONLY price (cheap `iter_anchors`+`extract_price`) + availability (`availability.offline_flags_from_text` on fresh card text), skipping full field detection + embedded-JSON parse + downstream VDP enrichment. Env knobs (all in `config.py`): `HTML_CACHE_ENABLE=1`, `HTML_CACHE_SIMILARITY=0.90`, `HTML_CACHE_MAX_AGE_DAYS=4` (rolling window; pruned at fetch-phase start). Tradeoff: a non-price field change carries over from last run until the page drops <90% or hits the 4-day expiry; SPA/embedded-JSON listings keep cached price on a hit. Cache dir is gitignored. Log marker on a hit: `[cache-hit 0.9xx] ... reused N listing(s)`. Landed on branch `feat/html-cache-fuzzy-refresh` (tests: `tests/test_htmlcache.py`).

**HTML-cache Docker persistence gotcha (critical):** the cache is WRITTEN during the fetch phase, but the fetcher container has NO host `./output` mount (isolation model) and `run.ps1` ends every run with `docker compose down -v` (wipes named volumes). So a naive `output/html_cache` write in the fetcher is EPHEMERAL — the cache never persists cross-run. Fix mirrors the snapshots flow: PLAN prunes the host store (4-day) + seeds the fetcher's quarantine copy (`htmlcache.sync(host → /quarantine/html_cache)`); FETCH reads/writes the quarantine copy (`SCRAPER_HTML_CACHE=/quarantine/html_cache` in compose); ANALYZE promotes it back to host `output/html_cache` (`htmlcache.sync(quarantine → host)`). `htmlcache.sync` is newer-wins (compares meta `fetched_ts`) so a stale copy never clobbers a fresh one — also makes local `--phase=all` safe. `config.HTML_CACHE_DIR` is env-overridable via `SCRAPER_HTML_CACHE` (defaults to host `OUTPUT_DIR/html_cache`).

**2026-07-11 — timing profiler (`scraper/timing.py`):** per-method wall-clock instrumentation to find bottlenecks. `timing.timed("name")` (context mgr) / `@timing.timed_fn()` (decorator) log `START name` / `DONE name in X.XXXs` via the `scraper.timing` logger (captured in `output/run.log`), and accumulate calls/total/max. Each phase calls `reset()`, `attach_file(<dir>/timing_<phase>.log)`, and at the end `log_report()` + `write_report(dir, phase, stamp)` → sorted-by-total table + `timing_<phase>.json`. Instrumented: PLAN/FETCH/ANALYZE phases; fetch per-source (`fetch.source.<SRC>`), per-winning-tier (`fetch.tier.<fast|human|api|unlocker>`), and `fetch.enrich.<SRC>`; analyze steps (safety/filter/dedupe/neardup/rank/scan/quality.*/url_align/availability/bucket/history/excel/report). Same container split as the cache: fetch writes `timing_fetch.*` to QUARANTINE, analyzer promotes to host `output/`. Artifacts gitignored (`output/timing_*.log|json`). Tests: `tests/test_timing.py`. Both features on branch `feat/html-cache-fuzzy-refresh`, uncommitted (local-only per user).

**2026-07-12 — concurrent fetch (biggest speed lever):** the fetch phase was sequential (~3hr for 60 sources, each capped by `SOURCE_BUDGET_S=360`). Now `cli/fetch.py` extracts the per-source loop body into `_fetch_one(idx,total,key,module,entry,cascade)` and runs it under a `ThreadPoolExecutor(max_workers=config.SOURCE_CONCURRENCY)`; results collected via `as_completed` (order-independent — analyze keys by `source_key`). Two-tier concurrency knobs (config.py, env-tunable): `SCRAPER_SOURCE_CONCURRENCY` (default 6) and `SCRAPER_BROWSER_CONCURRENCY` (default 2). Browser cap = module-level `threading.Semaphore` in `strategies/human.py` used as `with _BROWSER_SEM, sync_playwright() as p:` (one-liner, no re-indent) — REQUIRED so a burst of sources escalating to the Chromium/human tier doesn't OOM the 3g fetcher (each headless Chromium ~300-500MB). CRITICAL fix: `strategies/unlocker.py` paid-credit cap (`_calls_made`) had a check-then-increment race that could overspend real API credits under concurrency — now guarded by module `_CREDIT_LOCK` (atomic reserve-then-spend). Validated: 6 mixed-tier sources went 807s sequential → 181s concurrent (~4.5x), no OOM, data intact. Compose passes both knobs to the fetcher. Why threads (not async): Playwright here is sync API, and each `HumanStrategy.run` opens its own `sync_playwright()` context — thread-safe across threads, incompatible with a single async loop. Downstream analyze needed NO changes (already order-independent). Related zero-yield finding: 20 of 60 sources yield 0 listings (~28% of fetch time); concurrency overlaps their waste rather than eliminating it — a future lever is hard-skipping chronic-zero sources (bandit/remedy already track per-source success).

**2026-07-16 — every-state coverage push + negotiation-ready data (all committed to `main`, 9 commits, 133 tests):**
- **Removed the per-state cap.** `bucket.py` had `MAX_PER_BUCKET=15` that SILENTLY dropped overflow (a 118-truck run showed as 60; TX 50→15). Gone — every captured listing is now kept. `MAX_PER_BUCKET` deleted from bucket.py (still defined-but-unused in config.py).
- **State Coverage ledger** (`excel._write_state_coverage`, sheet right after README): one row per state (50+DC) with truck/2026/trim-family/series counts + the sources that found them + COVERED/EMPTY, and a summary that NAMES every empty state. The way to see the every-state gap each run.
- **Unlocker budget was unreachable:** `docker-compose.yml` only passed UNLOCKER_PROVIDER/API_KEY/ENDPOINT, so `UNLOCKER_MAX_TOTAL`(10)/`UNLOCKER_MAX_URLS`(4) were stuck at code defaults. Now passed through + set in `.env` (150/30). NOTE: the ZenRows key is a FREE TRIAL (~40 premium calls) — it does NOT crack cars.com/AutoTrader at scale. cars.com stays walled, so the user's actual AutoNation truck (2026 `3C63R3PL0TG332645`) is NEVER scraped; confirmed live manually at ~$91,497.
- **Breadth-first URL ordering (biggest FREE coverage lever):** the fast tier walks `URLS` in order and stops at `SOURCE_BUDGET_S`. Depth-first meant a cutoff (Craigslist hit 300s after 39 of 372 URLs) only reached alphabetically-early states. New `_base.interleave_by_state(specs)` groups by `state_hint` and round-robins by index → a 39-URL cutoff now touches 39 states, not 13. Applied to craigslist_nationwide, truecar_states, iseecars_states (iseecars/truecar also switched capital-only → `config.search_points()` = capital+metro).
- **Platform fingerprinting wired into the LIVE fast tier** (`strategies/fast.py`): the 1,052-dealer list already covers every state (8–34/state) — empty states are a WALL problem, not a list problem. `routing.detect_platform`/`paths_for` existed but were offline-only, AND the homepage probe only ran when `home_gate` (no session/unlocker) — so with the unlocker configured it never ran. Decoupled `probe_home=routed` (always) from the skip-gate; on a reachable dealer homepage, detect platform → try its known-good inventory path FIRST (logs `plat_hits`). Most dealers now solve via platform-path. Only helps homepage-reachable dealers; the ~44% hard-walled still need the (spent) unlocker.
- **Prioritized enrichment** (`enrich._enrich_priority`): `enrich_listings` took the first `ENRICH_MAX` candidates in list order, so thin dealer SRP cards ate the VDP budget (price coverage ~30%). Now sorts candidates by 2026 → Limited/Longhorn → 3500 → price-LESS-first (uses field values OR title/text hints) before the cap. Result: 2026-Limited TARGET subset hit 59% price / 63% VIN vs 22%/20% overall. `.env` also raised `SCRAPER_ENRICH_MAX`=60, `SCRAPER_ENRICH_BUDGET_S`=180.
- **Daily automated run** (`scheduled-run.ps1` + `register-daily-task.ps1`): Windows Scheduled Task "RamMegaCabScraper Daily" @ 03:30, headless, auto-starts Docker, no pause, logs to `output/scheduled-logs`. Coverage ACCUMULATES across runs (history DB persists) — any single free run covers ~14–20/51 (a DIFFERENT set each time), union grows. Disable: `Disable-ScheduledTask -TaskName "RamMegaCabScraper Daily"`.
- **Honest reality:** RAM 2500/3500 Mega Cab Limited/Laramie is a RARE ~$90k config (7.5k raw → ~260 after filter). Coverage went 6→20/51 states this session on free tiers alone. Getting a clean 51/51 in ONE run isn't feasible free (rare trim + walled aggregators that index every state's dealers); the daily task + accumulation is the free answer. 2026-Limited comps: ~13 priced, median ~$91,675, $80k–$149k — confirms the user's $86.5k target is below-market leverage.

**2026-07-13 — independent-review corrections (merged PR #3; supersedes some claims above):** (1) HTML cache NO LONGER stores the raw `<slug>.html` — only `<slug>.meta.json` (fingerprint + fp_len + listings). The `.html` was never read (fingerprint lives in meta) yet was copied host↔quarantine every run. sync/prune now use file mtime + `shutil.copy2` (no JSON parse); store is atomic (tmp+os.replace). (2) The cache-hit short-circuit was REWORKED: on a ≥0.90 hit it now STILL re-extracts the full current listing set (so new/removed inventory is never missed — the old "reuse last run's listing set" HID new trucks on stable pages) and only backfills last run's enrichment onto known listings via `_apply_cached_enrichment` (fields in `_ENRICH_FIELDS`; price always from the fresh page). Log line: `[cache-hit X] ... reuse cached enrichment`. (3) history "gone" (=> SOLD comp) now needs unseen ≥2 days (`SCRAPER_HISTORY_GONE_AFTER_DAYS`), else a blocked/subset run fabricates sold comps. (4) `Listing.__post_init__` coerces price/mileage/year to int-or-None (`models._coerce_int`); `history._connect` accepts str paths. (5) `timing._STATS` + fetch `_throttle` are now lock-safe under the concurrent pool. Plan phase dropped to ~18s (was an anomalous ~8min when copying the dead `.html`). All on `main`; test files: test_htmlcache/test_models/test_history (130 pass).
