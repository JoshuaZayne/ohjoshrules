---
name: project_spend_analysis_cc
description: "Running the Yearly_and_monthly_spend_per_CC repo — Python version gotcha, iCloud sync, and the two bank income accounts"
metadata: 
  node_type: memory
  type: project
  originSessionId: 6a2630be-b2cc-47a1-892a-0fa2a7eeee2c
  modified: 2026-08-03T07:01:19.679Z
---

Repo `Yearly_and_monthly_spend_per_CC` (cloned at `C:\Users\ohjos\repos\Yearly_and_monthly_spend_per_CC`) parses AMEX + Fidelity + Schwab statements into an Excel spend/income workbook.

**Python gotcha:** the code uses 3.10+ syntax (`str | None`), but this machine's default `python` is 3.9. Run everything with **`py -3.13`** — pandas/openpyxl/pdfplumber are installed there. `build_report.py` builds the workbook + trends md; `validate.py` runs the reconciliation suite (should be all-pass).

**iCloud source docs** live at **`F:\iCloudDrive\Documents`** on the desktop (drive-letter mount). Note a stale, empty `C:\Users\ohjos\iCloudDrive\Documents` mount also exists — `sync_check_icloud.py` scores candidate mounts by folder content so it picks F: automatically. Relevant folders: `Credit Card Annual`, `Bank Statements` (Amex/Charles Schwab/Fidelity), `Mortgage Statement` (Freedom Mortgage, ~$2,079.94/mo P&I), `VA LES Statements` (VA GS-civilian pay stubs), `VA W2s`. Run `python sync_check_icloud.py [--copy]` to mirror new iCloud docs into `source_documents/` so the repo isn't iCloud-dependent.

**Income model:** two Bank accounts feed money-in analysis — Fidelity Cash/Bank (VA disability/education benefits, VACP/VAED TREAS) and Charles Schwab Checking …086 (VA civilian DFAS salary + allotments). `classify_inflow` splits deposits into true income vs transfers between the user's own accounts. See [[project_thinkscript_version_tracking]] for the commit-each-change habit — same applies here (private repo tracks source_documents + generated output).

**iCloud moved → local-mirror fallback (2026-07-04):** the live `F:\iCloudDrive\Documents` is now EMPTY (the [[project_icloud_reorg]] moved most of it to `.Trash`). `config.find_icloud_documents` was hardened with a try-block + fallback to the local reorg mirrors `~/iCloudReorg/stage_raw_mirror_*/Documents`, newest full mirror first (**v2** over v1; live-only v3 scores 0 subfolders and is skipped). So the pipeline runs entirely from local copies now — no iCloud needed. `sync_check_icloud.py --copy` pulls from whichever mirror the resolver picks.

**Coverage guard (2026-07-04):** `analyze.housing_gap_months(df)` = single source of truth for interior months missing the mortgage (~$2k/mo anchor fixed cost) → Fixed/Expenses understated = data gap, not low spend. Shaded red in notebook sections 018/050/080 and the Excel Income-vs-Expenses chart. Known unfilled gap: **Fidelity 7832 checking Dec 2023–Jun 2024** (statements not local; `transactions.CSV` starts 2024-07 — needs a fresh Fidelity export). One-command refresh: `launch_notebooks_fresh.cmd`.

**RESOLVED (2026-07-12):** the 101-duplicate `validate.py` failure is GONE — a fresh `py -3.13 build_report.py` rebuild collapsed it (dedup now clean; validate = **150 checks, 0 failed**). validate's duplicate gate was also rewritten to threshold on EXCESS rows (Σ Times-1), not group count.

**Duplicate-CSV gotcha:** `source_documents/accounts/Charles Schwab (5086)/` currently holds TWO overlapping exports (`Main_Checking_XXX086_..._20260703-043117.csv` AND `...-193519.csv`). Every 2026 Schwab deposit appears twice in RAW records ($124,286 doubled); `analyze.dedup_transactions` correctly collapses to one (~$62k money-in / $56.5k income). Harmless (dedup handles it) but redundant — delete the older CSV to declutter. NOTE: an earlier stale export claimed 2026 income ~$124k; the real deduped figure is ~$68k, and it's INCOMPLETE (Fidelity 7832 shows only ~$7k of 2026 disability — most VACP disability deposits aren't imported → captured income badly understates true income; a coverage gap, not a bug).

**Projection code corrected (2026-07-12), `src/analyze.py` PROJ_* block (~line 1897):** now models MFJ 2025 OBBBA — std deduction 31,500, child credit 2,200/dep, and a NEW `PROJ_HSA_FAMILY=8550` pre-tax+FICA-exempt line (was missing). VA disability set to **$4,646.32/mo** ($55,755.84/yr, tax-free). Result: TOTAL TAX $28,872.72, NET incl. disability $221,833.11 ($18,486.09/mo). `_proj_effective_tax_rate(streams=...)` is now stream-scoped so pre-FUTU months (FUTU starts 2026-03) aren't taxed at the FUTU-inclusive rate. Tax math + income-vs-transfer split + housing imputation now covered by tests (`tests/test_projection.py`, `test_summaries.py`; suite = 65 passing).

**INCOME GAP RESOLVED (2026-08-02):** the long-standing "2026 VA disability missing / captured income badly understates true income" problem was NOT a bug — the VA deposits **moved accounts**. VACP/VAED TREAS deposits land in Fidelity **7832 checking through Aug 2025**, then in Fidelity brokerage **Z40-457979 from Sept 2025 onward**, which had never been imported. Adding `fidelity_investment_pdf` recovered **2026 VA benefits $0 → $31,618.62** and 2025 $48,817 → $66,784; total income 2025 +$19,304 and 2026 +$32,355. Z40 also pays the MACU 2nd mortgage ($374.57/mo), T-Mobile, and **United of Omaha life insurance $29.03/mo** (that stream runs Sept 2023 → now and was previously miscategorized as "Other Outflow"; 25 old rows were corrected to Insurance). Note **Freedom Mortgage ($2,079.94) genuinely stops after 2025-10-30** in the ledger — it is covered via `_invoice_fill_expenses` from the mortgage statements, not missing. See [[project_spend_intake_and_accounts]] for the Fidelity organizer.

**2024 BLACKOUT RESOLVED (2026-08-03):** the report showed **$0 spend for all of 2024** and only 30/44 months of card coverage. Cause was NOT missing documents — the NavyFed ANNUAL SUMMARY statements were already in `source_documents` but parsed to 0 rows (see [[project_spend_intake_and_accounts]]). Card coverage is now **54/55 months** (only the data edge missing) and card spend $172,322 → $327,604. `recurring_income_gaps` was also rewritten: it now matches a stream by CATEGORY+amount (so an account move is not reported as lost income), uses a **count-based shortfall** instead of date-window matching (VA pays on the last BUSINESS day, so two payments can land in one calendar month and none in the next — the old logic called that a gap), ignores sub-$25 dividend dust, and labels each row "still arriving" vs "STOPPED". Output went from 17 rows (mostly false) to 5 real ones.

**Other 2026-07-12 fixes:** `annual_summary`/`budget_summary` now IMPUTE the missing mortgage for interior housing-gap months (median housing × gap months) so net/savings aren't optimistic; category rules fixed (Target→Shopping not Groceries, bare "market" removed); NavyFed-CC parser now captures refunds/`CR` (signed), keeps its `ref`, and warns on zero-txn statements (loader handles signed amounts + cache `_v=2`); Fidelity-CC signs per-file via `meta["debit_is_positive"]`; new **`ICLOUD_REORG`** env override + Linux/Pi `ICLOUD_DOCS` note in `config.py`. Still UNCOMMITTED.
