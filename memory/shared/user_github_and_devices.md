---
name: User GitHub account and devices
description: User's GitHub identity, active devices, and where code lives across them
type: user
---

GitHub account: **JoshuaZayne** (the only account to use). Previously had `ohjoshrules` too, which caused account-picker prompts — git global `user.name` has been set to `JoshuaZayne` to fix this. If any remote still points to `ohjoshrules/...`, update it to `JoshuaZayne/...`.

Devices the user develops on (code must work across all three):
1. **Desktop** (Windows) — home at `C:\Users\ohjos`, iCloud at `C:\Users\ohjos\iCloudDrive`
2. **Laptop** (Windows) — uses external storage, iCloud at `F:\iCloudDrive`, many repos cloned directly under `F:\`
3. **Raspberry Pi** (Linux) — home likely `/home/pi` or `/home/ohjos`

There is also a **D: drive USB** (SanDisk) that moves between machines with repos under `D:\Code\`. It's not a "device" per se but code needs to handle it.
